name: keepalive
on:
  schedule:
    - cron: "*/10 * * * *"   # every 10 minutes (UTC)
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render health
        run: |
          curl -fsS https://loncapa-autosolver-3.onrender.com/health -o /dev/null || true
